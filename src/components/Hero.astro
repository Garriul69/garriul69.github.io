---
const techPills = [
  'Intune',
  'CrowdStrike',
  'Entra ID',
  'PowerShell',
  'Autopilot',
];
---

<section id="hero" class="hero">
  <!-- Particle canvas -->
  <canvas id="particle-canvas" class="hero__canvas" aria-hidden="true"></canvas>

  <!-- Content -->
  <div class="hero__content">
    <!-- Live badge -->
    <div class="hero__badge" data-anim="fade-up" style="--delay:0s">
      <span class="hero__badge-dot" aria-hidden="true"></span>
      <span class="hero__badge-text">IT INFRASTRUCTURE &amp; SECURITY ENGINEER</span>
    </div>

    <!-- Name headline -->
    <h1 class="hero__name" aria-label="Andy Gibran López Nambo">
      <span class="hero__name-line" id="name-line-1">
        <span class="word"><span class="name-word">Andy</span></span>&nbsp;<span class="word"><span class="name-word">Gibran</span></span>
      </span>
      <span class="hero__name-line" id="name-line-2">
        <span class="word"><span class="name-word hero__name--accent">López</span></span>&nbsp;<span class="word"><span class="name-word">Nambo</span></span>
      </span>
    </h1>

    <!-- Subheadline -->
    <p class="hero__sub" data-anim="fade-up" style="--delay:0.55s">
      Securing and modernizing enterprise infrastructure from the endpoint up.
    </p>

    <!-- Tech pills -->
    <div class="hero__pills" data-anim="fade-up" style="--delay:0.7s" aria-label="Core technologies">
      {techPills.map((pill, i) => (
        <span class="hero__pill" style={`--pill-delay:${i * 0.06}s`}>{pill}</span>
      ))}
    </div>

    <!-- CTAs -->
    <div class="hero__ctas" data-anim="fade-up" style="--delay:0.9s">
      <a href="#projects" class="btn-magnetic btn-primary hero__cta-btn">
        View Projects <span aria-hidden="true">→</span>
      </a>
      <a href="#contact" class="btn-magnetic btn-outline hero__cta-btn">
        Contact Me
      </a>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="hero__scroll" aria-hidden="true" data-anim="fade-up" style="--delay:1.2s">
    <span class="hero__scroll-text">SCROLL</span>
    <div class="hero__scroll-chevrons">
      <svg width="14" height="8" viewBox="0 0 14 8" fill="none" aria-hidden="true">
        <path d="M1 1l6 6 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <svg width="14" height="8" viewBox="0 0 14 8" fill="none" aria-hidden="true">
        <path d="M1 1l6 6 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: var(--navbar-h) 1.5rem 0;
  }

  .hero__canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .hero__content {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
    padding: 4rem 0 6rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 2rem;
  }

  /* Badge */
  .hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    padding: 0.35rem 0.9rem;
    opacity: 0;
    transform: translateY(12px);
  }
  .hero__badge-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--color-accent);
    box-shadow: 0 0 8px var(--color-accent);
    animation: pulse-dot 2.5s ease-in-out infinite;
  }
  @keyframes pulse-dot {
    0%, 100% { opacity: 1; box-shadow: 0 0 8px var(--color-accent); }
    50%       { opacity: 0.55; box-shadow: 0 0 4px var(--color-accent); }
  }
  .hero__badge-text {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.14em;
    color: var(--color-muted);
  }

  /* Name */
  .hero__name {
    /* DM Sans: normal-width bold — avoids the stretched look of the wide Syne display face */
    font-family: var(--font-body);
    font-size: clamp(2.75rem, 8vw, 6.5rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.025em;
    color: var(--color-text);
    margin: 0;
  }
  .hero__name-line { display: block; }
  /* Per-word mask: padding buffer prevents clipping of descenders/accents */
  .hero__name .word {
    overflow: hidden;
    padding-block: 0.1em;
    margin-block: -0.1em;
    vertical-align: top;
  }
  .name-word {
    display: inline-block;
  }
  .hero__name--accent { color: var(--color-accent); }

  /* Sub */
  .hero__sub {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--color-muted);
    max-width: 520px;
    line-height: 1.6;
    opacity: 0;
    transform: translateY(12px);
  }

  /* Pills */
  .hero__pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    opacity: 0;
    transform: translateY(12px);
  }
  .hero__pill {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    color: var(--color-secondary);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    padding: 0.3rem 0.75rem;
    background: rgba(14, 165, 233, 0.05);
    transition: border-color 0.2s, background 0.2s;
  }
  .hero__pill:hover {
    border-color: var(--color-secondary);
    background: rgba(14, 165, 233, 0.1);
  }

  /* CTAs */
  .hero__ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    opacity: 0;
    transform: translateY(12px);
  }
  .hero__cta-btn {
    font-size: 1rem;
    padding: 0.8rem 2rem;
  }

  /* Scroll indicator */
  .hero__scroll {
    position: absolute;
    bottom: 2.5rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
    opacity: 0;
  }
  .hero__scroll-text {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.18em;
    color: var(--color-muted);
  }
  .hero__scroll-chevrons {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    color: var(--color-muted);
    animation: bounce-chevrons 1.8s ease-in-out infinite;
  }
  @keyframes bounce-chevrons {
    0%, 100% { transform: translateY(0); opacity: 0.5; }
    50%       { transform: translateY(5px); opacity: 1; }
  }

  @media (max-width: 640px) {
    .hero__content { padding: 2rem 0 5rem; gap: 1.5rem; }
    .hero__cta-btn { padding: 0.7rem 1.5rem; font-size: 0.9375rem; }
  }
</style>

<script>
  import { gsap } from 'gsap';

  // ── Particle system ─────────────────────────────────────────
  const canvas = document.getElementById('particle-canvas') as HTMLCanvasElement;
  const ctx = canvas.getContext('2d')!;

  type Particle = {
    x: number; y: number;
    vx: number; vy: number;
    r: number; alpha: number;
    color: string;
  };

  const COLORS = ['#3B82F6', '#0EA5E9', '#1d4ed8'];
  let W = 0, H = 0;
  let particles: Particle[] = [];
  let mouse = { x: -9999, y: -9999 };
  let animFrame: number;

  const resize = () => {
    W = canvas.width  = canvas.offsetWidth;
    H = canvas.height = canvas.offsetHeight;
  };

  const spawn = (): Particle => ({
    x: Math.random() * W,
    y: Math.random() * H,
    vx: (Math.random() - 0.5) * 0.35,
    vy: (Math.random() - 0.5) * 0.35,
    r: Math.random() * 1.6 + 0.4,
    alpha: Math.random() * 0.6 + 0.1,
    color: COLORS[Math.floor(Math.random() * COLORS.length)],
  });

  const init = () => {
    const count = Math.min(Math.floor((W * H) / 14000), 120);
    particles = Array.from({ length: count }, spawn);
  };

  const LINK_DIST = 140;

  const draw = () => {
    ctx.clearRect(0, 0, W, H);

    // Update + draw dots
    particles.forEach(p => {
      p.x += p.vx;
      p.y += p.vy;
      if (p.x < 0) p.x = W;
      if (p.x > W) p.x = 0;
      if (p.y < 0) p.y = H;
      if (p.y > H) p.y = 0;

      // Mouse repulsion
      const dx = p.x - mouse.x;
      const dy = p.y - mouse.y;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist < 90) {
        p.x += (dx / dist) * 1.2;
        p.y += (dy / dist) * 1.2;
      }

      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = p.color + Math.floor(p.alpha * 255).toString(16).padStart(2, '0');
      ctx.fill();
    });

    // Draw connecting lines
    for (let i = 0; i < particles.length; i++) {
      for (let j = i + 1; j < particles.length; j++) {
        const dx = particles[i].x - particles[j].x;
        const dy = particles[i].y - particles[j].y;
        const d = Math.sqrt(dx * dx + dy * dy);
        if (d < LINK_DIST) {
          const alpha = (1 - d / LINK_DIST) * 0.18;
          ctx.beginPath();
          ctx.moveTo(particles[i].x, particles[i].y);
          ctx.lineTo(particles[j].x, particles[j].y);
          ctx.strokeStyle = `rgba(59,130,246,${alpha})`;
          ctx.lineWidth = 0.7;
          ctx.stroke();
        }
      }
    }

    animFrame = requestAnimationFrame(draw);
  };

  resize();
  init();
  draw();

  window.addEventListener('resize', () => { resize(); init(); }, { passive: true });
  window.addEventListener('mousemove', e => {
    mouse.x = e.clientX;
    mouse.y = e.clientY;
  }, { passive: true });

  // ── GSAP entrance animations ────────────────────────────────
  // Set hidden state via JS so text stays visible if JS ever fails to run.
  gsap.set('.name-word', { yPercent: 120, opacity: 0 });

  const tl = gsap.timeline({ defaults: { ease: 'power3.out' } });

  // Badge fade-up
  tl.to('.hero__badge', { opacity: 1, y: 0, duration: 0.6 }, 0.15)

  // Name: words slide up from clip
  .to('.name-word', {
    yPercent: 0,
    opacity: 1,
    duration: 0.75,
    stagger: 0.12,
  }, 0.35)

  // Subheadline
  .to('.hero__sub', { opacity: 1, y: 0, duration: 0.6 }, 0.65)

  // Pills
  .to('.hero__pills', { opacity: 1, y: 0, duration: 0.5 }, 0.78)

  // CTAs
  .to('.hero__ctas', { opacity: 1, y: 0, duration: 0.5 }, 0.9)

  // Scroll indicator
  .to('.hero__scroll', { opacity: 1, duration: 0.6 }, 1.1);

  // ── Magnetic button effect ──────────────────────────────────
  document.querySelectorAll<HTMLElement>('.btn-magnetic').forEach(btn => {
    btn.addEventListener('mousemove', (e) => {
      const rect = btn.getBoundingClientRect();
      const x = e.clientX - rect.left - rect.width  / 2;
      const y = e.clientY - rect.top  - rect.height / 2;
      gsap.to(btn, { x: x * 0.28, y: y * 0.28, duration: 0.3, ease: 'power2.out' });
    });
    btn.addEventListener('mouseleave', () => {
      gsap.to(btn, { x: 0, y: 0, duration: 0.5, ease: 'elastic.out(1,0.5)' });
    });
  });
</script>
